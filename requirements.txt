# Telegram bot framework / Фреймворк для Telegram-бота
aiogram==3.30.0

# The downloader itself. Pinned here, but the container can pull a fresher
# build at startup — see AUTO_UPGRADE_YTDLP and YTDLP_CHANNEL.
# Власне завантажувач. Версія зафіксована, але контейнер може підтягнути
# свіжішу збірку при старті — див. AUTO_UPGRADE_YTDLP і YTDLP_CHANNEL.
#
# The "curl-cffi" extra is what lets yt-dlp mimic a real browser's TLS
# fingerprint. Without it TikTok answers 403 Forbidden to every request,
# even with valid cookies — see entrypoint.sh for the same extra on upgrade.
# Екстра "curl-cffi" дає yt-dlp змогу вдавати TLS-відбиток справжнього
# браузера. Без неї TikTok відповідає 403 Forbidden на будь-який запит,
# навіть із дійсними cookies — та сама екстра є і в entrypoint.sh.
yt-dlp[default,curl-cffi]==2026.7.4

# PO token provider for getting past YouTube protection
# (works together with the bgutil-provider container).
# Провайдер PO-токенів для обходу YouTube-захисту
# (працює в парі з контейнером bgutil-provider).
bgutil-ytdlp-pot-provider
